################################################################################
# FinOps AWS - Backend Configuration Example
# 
# Para usar um backend S3 para o state do Terraform:
# 1. Crie o bucket e tabela DynamoDB (veja README_TERRAFORM.md)
# 2. Renomeie este arquivo para backend.tf
# 3. Ajuste os valores abaixo
# 4. Execute: terraform init -migrate-state
################################################################################

terraform {
  backend "s3" {
    bucket         = "finops-aws-terraform-state"
    key            = "finops-aws/terraform.tfstate"
    region         = "us-east-1"
    encrypt        = true
    dynamodb_table = "finops-aws-terraform-locks"
    
    # Descomente se usar assume role
    # role_arn = "arn:aws:iam::ACCOUNT_ID:role/TerraformRole"
  }
}

################################################################################
# Script para criar os recursos do backend:
#
# aws s3 mb s3://finops-aws-terraform-state --region us-east-1
#
# aws s3api put-bucket-versioning \
#   --bucket finops-aws-terraform-state \
#   --versioning-configuration Status=Enabled
#
# aws s3api put-bucket-encryption \
#   --bucket finops-aws-terraform-state \
#   --server-side-encryption-configuration '{
#     "Rules": [{
#       "ApplyServerSideEncryptionByDefault": {
#         "SSEAlgorithm": "AES256"
#       }
#     }]
#   }'
#
# aws s3api put-public-access-block \
#   --bucket finops-aws-terraform-state \
#   --public-access-block-configuration '{
#     "BlockPublicAcls": true,
#     "IgnorePublicAcls": true,
#     "BlockPublicPolicy": true,
#     "RestrictPublicBuckets": true
#   }'
#
# aws dynamodb create-table \
#   --table-name finops-aws-terraform-locks \
#   --attribute-definitions AttributeName=LockID,AttributeType=S \
#   --key-schema AttributeName=LockID,KeyType=HASH \
#   --billing-mode PAY_PER_REQUEST \
#   --region us-east-1
################################################################################
